{
  "name": "tools-figma-downloader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Download a large Figma file in chunks (stream batched node subtrees to disk) and reconstruct a single full file JSON locally. Zero-dependency Node ESM (Node >= 20 built-ins only).",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "figma-download": "./bin/figma-download.mjs"
  },
  "scripts": {
    "start": "node bin/figma-download.mjs",
    "test": "node --test",
    "typecheck": "node --check bin/figma-download.mjs && node --check src/cli.mjs && node --check src/figmaClient.mjs && node --check src/chunk.mjs && node --check src/reconstruct.mjs && node --check src/util.mjs && node --check src/httpFetch.mjs && node --check src/mock.mjs",
    "lint": "npm run typecheck"
  }
}
